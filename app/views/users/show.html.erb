<% provide:title, @user.name %>
<div class="col-md-8">
  <% if @microposts.any? %>
    <h3>Microposts (<%= @microposts.count %>)</h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
  <% end %>
</div>


